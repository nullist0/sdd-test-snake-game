package com.snakegame.domain.model

/**
 * Represents a position on the game grid.
 *
 * @property x The horizontal coordinate (column)
 * @property y The vertical coordinate (row)
 */
data class Position(
    val x: Int,
    val y: Int
)
